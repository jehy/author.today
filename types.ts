export type ConvertOptions = {
  authors:string,
  title:string,
  pageBreaksBefore: string,
  chapter: string,
  insertBlankLine: boolean,
  insertBlankLineSize: string,
  lineHeight: string,
  cover?: string,
};

export type BookMeta = { authors:string, title:string };
